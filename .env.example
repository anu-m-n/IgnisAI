# Choose one provider: "anthropic" or "gemini"
LLM_PROVIDER=anthropic

# Required if LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# Required if LLM_PROVIDER=gemini
GEMINI_API_KEY=

# Optional overrides
ANTHROPIC_MODEL=claude-sonnet-4-6
GEMINI_MODEL=gemini-2.5-flash

PORT=3000
